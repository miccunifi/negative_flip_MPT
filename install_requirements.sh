#!/bin/bash

# PyTorch and torchvision should be installed via conda as per README.
# Install only project-specific pip packages not covered by the conda step.
pip install numpy matplotlib scikit-learn tqdm scipy pillow